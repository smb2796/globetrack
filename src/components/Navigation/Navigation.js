import React from 'react';

const Navigation = () => {
	return (
			<nav style={{display: 'flex', justifyContent: 'center'}}>
				<p style={{justifyContent: 'center'}} className='f3 link dim black underline pa3 pointer'> GLOBETRACK.IO </p>
			</nav>
	);
}

export default Navigation;